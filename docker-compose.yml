version: "3.8"

services:
  "null":
    build: .
    image: ghcr.io/shniranjan/null:latest
    container_name: null
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - XCPNG_DB_PATH=/data/null.db
    volumes:
      - ./data:/data
    restart: unless-stopped
